# Real-time React client
React single page app for real-time data

## Installation instructions
In the project's directory, run the following commands:

```
$ npm install
$ npm run build
$ npm start
```
